'use client';

import * as React from 'react';

// Placeholder: a client-only table region where you can hydrate with SWR later.
// Keeping this as a separate client file ensures no server onClick leaks.
export default function TransactionsTableClient() {
  return null;
}
